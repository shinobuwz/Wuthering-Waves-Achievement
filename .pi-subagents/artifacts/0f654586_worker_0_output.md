The red phase is now observed at the intended boundary: after restore, the focused test project fails to compile because `AchievementWorkspace`, `Achievement`, `AchievementId`, and status/result contracts do not exist. I’m implementing that seam next, including revision-preserving failures and grouped statistics so the WPF layer remains a consumer rather than a second behavior implementation.