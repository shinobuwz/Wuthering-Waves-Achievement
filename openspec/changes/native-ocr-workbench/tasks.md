## 1. 扫描任务契约与编排抽离

- [ ] 1.1 定义 `OcrScanMode`、`OcrScanProgress`、`OcrScanRunResult` 和统一取消/错误结果模型；进度快照不得暴露可变内部集合。
- [ ] 1.2 从 `MainWindow.xaml.cs` 抽取当前分类扫描和全量扫描的公共初始化、资源释放、取消检查、预览结果汇总逻辑。
- [ ] 1.3 建立 `OcrScanCoordinator`（或等价应用层服务），让当前分类和全量扫描通过统一入口报告阶段、分类、页码、匹配数、未匹配数和警告。
- [ ] 1.4 保持现有扫描语义：当前分类仍只扫描当前分类，全量扫描仍按一级/二级分类导航并滚动；不改变模型、坐标和终止条件。
- **验收：** coordinator 能在不启动 WPF 窗口的情况下被 fake capture/reader 驱动，成功、取消和失败都返回结构化结果；取消或失败不会调用 workspace 写入。

## 2. OCR 工作窗口

- [x] 2.1 新增 `OcrScanWindow.xaml` 和代码后置，显示扫描模式、当前阶段、一级/二级分类、页码、累计匹配数、未匹配数和最近消息。
- [x] 2.2 增加“取消扫描”按钮、运行中/已取消/失败/已完成状态，以及可恢复警告列表或摘要区域。
- [x] 2.3 使用 DynamicResource 适配深色/浅色主题，并为模式、阶段、统计、取消和摘要控件增加 AutomationId。
- [x] 2.4 明确处理窗口关闭：运行中关闭等价于请求取消，等待任务收尾后再关闭；不得留下无法再次启动的忙碌状态。
- **验收：** 工作窗口在 1080×700 和 1440×900 两种目标尺寸下无裁剪、重叠或不可见的取消控件。

## 3. 主窗口集成

- [x] 3.1 将“OCR 自动扫描当前分类”和“OCR 全量扫描所有分类”按钮改为启动对应模式的 `OcrScanWindow`，不新增顶层 Tab。
- [x] 3.2 将现有 `HintText` 进度更新迁移为工作窗口进度更新；主页面只保留简短的启动、取消、完成或失败摘要。
- [x] 3.3 保持现有游戏窗口保护行为：扫描期间不遮挡游戏，任务结束或取消后恢复工具窗口；确保同权限级别下的输入自动化不变。
- [x] 3.4 扫描成功后继续打开现有 `OcrPreviewWindow`；只有明确接受预览后才调用 `ApplyOcrPreviewAsync`，接受后刷新主页面。
- **验收：** 当前分类和全量模式都能从主页面启动，任务结束后主页面可继续筛选、编辑和再次启动 OCR。

## 4. 测试与诊断

- [ ] 4.1 为 coordinator 增加当前分类/全量进度顺序、分类警告、取消、失败清理和 no-write-before-preview 测试。
- [ ] 4.2 为进度快照和结果汇总增加边界测试：空结果、重复页、跳过分类、未匹配文本、重复取消。
- [ ] 4.3 扩展 WPF UI Automation 验证：两个按钮打开正确模式、工作窗口控件可达、取消后状态恢复、主题切换后文字可读。
- [ ] 4.4 更新 native OCR 运行说明，明确扫描期间的游戏窗口、权限级别、取消行为和预览确认流程。
- **验收：** `dotnet test native/WutheringWavesAchievement.sln -c Release`、Release build 和 UI 验收脚本通过。

## 5. 手工验收

- [ ] 5.1 在工具和游戏相同权限级别下执行一次当前分类扫描，确认工作窗口持续报告进度并能取消。
- [ ] 5.2 执行一次全量扫描，确认一级/二级分类切换、分页滚动、跳过警告和最终摘要均可见。
- [ ] 5.3 在预览窗口取消和接受各执行一次，分别确认 workspace revision 不变和仅在接受后更新。
